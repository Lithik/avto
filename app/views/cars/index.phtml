<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
use Phalcon\Tag; ?>

<div class="page-header">
    <h2>
        Все автомобили
    </h2>
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["cars/new", "Добавить автомобиль "]); ?></li>           
        </ul>
    </nav>
</div>


<?php echo $this->getContent() ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id автомобиля</th>
            <th>Id пользователя</th>
            <th>Автомобиль</th>
            <th>Цена</th>
            <th>Фотография</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $order): ?>
            <tr>
                <td><?php echo $order->_id ?></td>
            <td><?php echo $order->id_user ?></td>
            <td><?php echo $order->avto ?></td>
            <td><?php echo $order->price ?></td>
            <td><div class="image"><?php echo $this->tag->image("img/$order->img"); ?></div></td>


                <td><?php echo $this->tag->linkTo(["cars/edit/" . $order->_id, "Редактировать"]); ?></td>
                <td><?php echo $this->tag->linkTo(["cars/delete/" . $order->_id, "Удалить"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>
<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("cars", "Первая") ?></li>
                <li><?php echo $this->tag->linkTo("cars?page=" . $page->before, "Предыдущая") ?></li>
                <li><?php echo $this->tag->linkTo("cars?page=" . $page->next, "Следующая") ?></li>
                <li><?php echo $this->tag->linkTo("cars?page=" . $page->last, "Последняя") ?></li>
            </ul>
        </nav>
    </div>
</div> 

<?php echo $this->tag->endForm(); ?>
